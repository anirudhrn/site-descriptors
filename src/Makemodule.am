bin_PROGRAMS+=site-descriptors

site_descriptors_SOURCES=./src/main.cpp

#You probably don't need all (or any!) of these, but just in case they're being added.
#If you installed the casm libraries you already have them installed so YOLO
site_descriptors_LDADD=$(BOOST_SYSTEM_LIB)\
				   $(BOOST_FILESYSTEM_LIB)\
				   $(BOOST_PROGRAM_OPTIONS_LIB)\
				   $(BOOST_REGEX_LIB)\
				   $(BOOST_CHRONO_LIB)


